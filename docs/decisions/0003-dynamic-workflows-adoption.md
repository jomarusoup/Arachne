---
Title: "ADR-0003 Dynamic workflows 도입과 3-lane 배치"
creation: 2026-08-22
modification: 2026-08-22
tags:
 - "arachne"
 - "decision"
 - "workflow"
 - "multi-cli"
aliases:
 - "adr-dynamic-workflows"
---
MOC:: [[Arachne]]
FROM:: [[2026-08-22-harness-runtime-audit]]

# ADR-0003: Dynamic workflows 도입과 3-lane 배치

## 상태

Proposed (채택안 제시 — 사람 승인 대기)

## 결정 질문

Arachne에 Claude Code dynamic workflows(스크립트 기반 다중 에이전트 오케스트레이션, 동시 16
에이전트 규모)를 도입할 것인가. 도입한다면 3-lane 위임 구조(Claude 중심 / Codex tester / Gemini
reader)와 어떤 관계로 배치할 것인가.

## 전제 (감사 `2026-08-22-harness-runtime-audit` 근거)

1. **Q1 미해결 — 컨텍스트 조립 관측 수단 부재(B-10).** rules가 서브에이전트에 주입되는지 실험
   B·보조 수단 모두 (b) 관측 불가로 종료. 본 ADR의 모든 완화책은 **주입 여부 어느 쪽이어도
   성립**해야 한다.
2. **상태 파일 경합 일체** — 무잠금 lost update(C-01), check→act TOCTOU로 쿼터 이중 소모(C-03),
   cross-fs `mv` 비원자(C-02), 비수치 값 무방어(C-04). 저장소 전체에 잠금 0건, 경합 테스트 0건.
3. **git-bus-check의 worktree 상태 분리(C-05·C-06)** — `/worktree` 병렬 실행은 도입 이전에 이미
   동료 커밋 재공지 오탐과 동일 폴더 경합 미탐을 낸다.
4. **B-11 스냅샷 덮어쓰기** — 세션 다중화 자체가 이미 데이터 유실을 낳는다(프로브 실측: 927B
   스냅샷이 426B "브랜치: N/A"로 대체, 복구 불가).
5. [현행 결함] **B-01~B-11**, [도입 제약] **C-01~C-10** 전체 (감사 보고서 "결함 분류" 절).
6. 실측 비용 기준점: 트리비얼 프롬프트 기준 서브에이전트 콜드 스타트 캐시 생성 **25.1k**(planner/
   opus)·**27.9k**(code-reviewer/sonnet) 입력 토큰. 16 에이전트 기본 오버헤드 하한 **≈0.4M 입력
   토큰/실행** [추정 — 실측 콜드 스타트 × 16, 작업 토큰 제외]. 대조: `gtask` 오프로드의 Claude 측
   토큰 소모는 ≈0(래퍼 호출 텍스트뿐).

## 선택지 비교

각 선택지에 대해 감사 지정 6개 질문에 답한다.

### (A) 도입하지 않는다 — 현행 3-lane 유지

1. **깨지는 것**: 신규로는 없음(동시 16을 얹지 않음). 단 "현상 유지 = 무결함"이 아님 — C-05·C-06은
   `/worktree` 다중 세션에서, B-11은 세션 2개(프로젝트 무관)에서 이미 발현 중. B-01~11은 도입과
   무관하게 수리 대상으로 잔존.
2. 오프로드 사슬 유지. 비용 변화 **0**.
3. 유지 — 단일 세션 단일 diff, 압축 메커니즘 불요.
4. C-08 이관은 (A)에서도 여전히 권장(가치가 도입과 무관)이나 규모 압력이 없어 우선순위만 낮아짐.
5. 충돌 없음.
6. 해당 없음 — 단 B-10은 "현행 규율이 적용되는지 스스로 검증 불가"라는 공백으로 남는다.
- **기회비용**: 대규모 감사·전수 검증·마이그레이션을 사람이 worktree 세션으로 수동 병렬화 —
  세션마다 사람 개입 필요, 그 수동 병렬이 곧 C-05 오탐·B-11 유실의 발생 경로다.

### (B) 선결조건 해소 후 도입

1. **깨지는 것(선결조건 미충족 상태에서 얹을 경우)**: C-01 쿨다운 기록 소실 → 소진 CLI 재타격,
   C-03 최대 16× 쿼터 이중 소모(사후 기록 구조라 원천 방지 불가), C-02/C-04 부분 기록 → 가용 오판,
   비격리 에이전트 2개의 동일 파일 Edit 충돌, 다중 세션 병용 시 B-11 유실. **충족 후**: 각 항목이
   아래 PC 체크리스트의 테스트로 봉인되므로 깨지지 않는다 — 근거는 서술이 아니라 통과하는 테스트.
2. **조건부 무력화**: read-heavy 단계를 워크플로에 넣으면 사슬 우회(에이전트당 25k+ 하한 vs Gemini
   0). 정책으로 read-heavy를 `gtask`에 남기면 비용 증가는 검증·리뷰 단계에 국한 — **+0.4M 하한/실행**.
3. **유지 가능 — 조건 3개**: ① 수정형 워크플로는 worktree 격리 의무, ② 종단 synthesis 단계가
   "파일별 변경 요약 + worktree별 `/verify` 결과"를 단일 문서로 산출, ③ 커밋은 메인 세션이 worktree
   단위 순차 수행. 사람 검증 대상은 N개 원시 diff가 아니라 (a) synthesis 요약 (b) verify 판정표
   (c) 고위험 diff 스팟체크 1~2건 — 이 압축 계약을 해당 커맨드 문서에 명문화한다.
4. 아래 PC-7 (구조 계층 배치는 (D)의 4번과 동일 원칙).
5. 충돌 없음 — 조건: 워크플로 서술은 CLAUDE.md·커맨드 계층에만 두고 `AGENTS.md` 본문 불변.
6. 허용 — 근거: 게이트를 관측 불가한 컨텍스트가 아니라 **관측 가능한 산출물**(CI·verify 리포트·
   실행 기록)에 둔다. B-10은 디버깅 제약으로 남지만 도입 차단 사유는 아니다.

**선결조건 체크리스트** (각 항목의 완료 판정은 검증 가능 형태):

- [ ] **PC-1** (C-01·C-03): `tests/`에 동시 10개 이상 프로세스가 서로 다른 CLI로 `SetCooldown`을
  수행하는 경합 bats 테스트가 존재하고, 종료 후 모든 CLI의 쿨다운 기록이 보존됨을 검증하며 통과한다.
- [ ] **PC-2** (C-02): 상태 파일 교체 중 병렬 reader가 빈 값·부분 라인을 관측하지 않음을 검증하는
  테스트가 존재하고 통과한다 (임시 파일이 상태 디렉터리와 같은 파일시스템에서 rename됨을 포함 검증).
- [ ] **PC-3** (C-04): 상태 파일에 비수치 값을 주입한 픽스처에서 쿨다운 판정이 bash 오류 없이 안전
  기본값으로 처리됨을 검증하는 테스트 통과.
- [ ] **PC-4** (B-01~03): stdout에 "429"·"rate limit"이 포함된 비쿼터 실패 픽스처에서 쿨다운이
  등록되지 않음을 검증하는 회귀 테스트 통과, 그리고 이슈 `workflow-06-quota-false-positive`가
  재오픈 → 코드 반영 → 종결 상태다.
- [ ] **PC-5** (B-04·B-05): 업스트림 미설정 브랜치에서 커밋 시 git-bus 무공지, upstream 리라이트 시
  경고 출력 — 각각 bats 테스트로 검증되고 통과한다.
- [ ] **PC-6** (B-11): 비-git CWD 헤드리스 세션 종료가 기존 auto-스냅샷을 덮어쓰지 않음(또는
  세션·프로젝트 구분 저장)을 검증하는 테스트 통과.
- [ ] **PC-7** (C-08): `settings.template.json`에 파괴적 도구 호출을 차단하는 PreToolUse 훅이
  등록돼 있고, 최소 `commands/git.md`·`commands/worktree.md`에 `allowed-tools` frontmatter가
  존재하며, `tests/validate_settings.sh`(또는 동급 인덱스 검사)가 두 사실을 검사하고 통과한다.
- [ ] **PC-8** (Q2 잔여): PreToolUse·PostToolUse가 서브에이전트 도구 호출에 발화하는지 실험 1회
  완료, 결과가 감사 보고서 Q2에 기록됨. 발화하지 않는 것으로 확정되면 PC-7의 훅 커버리지를 CI
  게이트로 보강하는 항목이 체크리스트에 추가된다.
- [ ] **PC-9** (Q1/B-10 완화): `agents/` 8종 본문에 핵심 계약(커밋 금지·역할 제한·보고 형식) 명시
  블록이 존재하고, 형식 검사 테스트(`tests/` 소재)가 이를 검증하며 통과한다 — 주입 여부와 무관하게
  성립하는 완화.
- [ ] **PC-10** (B-10): 워크플로 실행의 산출 기록(에이전트별 반환값·요약)을 저장소 감사 가능 위치에
  남기는 절차가 문서화되고, 스모크 실행 1회의 기록이 실재한다.

### (C) 배타적 도입 — 워크플로 실행 중 gtask/ctask 위임 금지

1. C-01~04는 "호출 금지"로 회피되는 듯 보이나 금지 자체가 강제 불가(4번) → 회피 보장 없음. 확실히
   깨지는 것: **Claude 쿼터 집중** — 16 에이전트 × (25k 콜드 스타트 + 작업 토큰)이 전부 단일 5h
   창에서 소모되고, 소진 시 `COOLDOWN_CLAUDE=18000s`(5h) 동안 중심 레인이 정지한다. 가용성 폴백
   사슬의 존재 이유가 워크플로 구간에서 소멸.
2. **구간 한정 전면 무력화.** 현재 Claude 토큰 ≈0인 read-heavy 작업이 회당 25k+ 로 이동 — 대용량
   읽기 8건 기준 **+200k~수백k/실행** 추가 [추정].
3. 유지되나 (B)의 압축 계약이 동일하게 필요 — 금지와 무관한 별도 문제.
4. **강제 불가 판정이 이 선택지의 기각 사유다.** "워크플로 실행 중"이라는 상태를 hooks·
   allowed-tools·CI 어느 계층도 식별할 수 없다(컨텍스트 계층 관측 불가 — B-10과 동류). 스크립트
   내부 선언은 매 실행 재생성이라 강제력이 없다(결정 질문의 전제). 즉 (C)는 C-08을 해소하지 않고
   **프롬프트 의존 금지 규칙을 하나 더 추가**한다.
5. Claude 레인만 능력이 갈라지는 것 자체는 `AGENTS.md:173-179`(집행 수단 차이 선언)가 이미
   허용하나, 오프로드 사슬을 구간 정지시키는 것은 `rules/common/workflow.md`의 두 사슬(비용·가용성)
   계약과 **실질 충돌**한다.
6. 준수 여부를 관측할 수 없는 금지 규칙(4·B-10) — **자기모순**. 허용 불가.

### (D) 계층 분리 — 특정 커맨드에서만 워크플로 활성화

범위 정의: **읽기 전용·검증 성격 커맨드**(감사, 전수 검증, 다차원 리뷰, 대규모 조사)에만 워크플로
허용. 수정형 작업은 현행 3-lane 유지.

1. **깨지는 것 없음 — 조건부**: 읽기 전용이면 파일 충돌·git 경합이 원천 부재(쓰기 없음). 상태
   경합은 에이전트가 atask/래퍼를 부르지 않는 한 없음 — 이 경계도 프롬프트 계층이지만 blast
   radius가 "부정확한 보고서"로 한정되고 산출물이 사람 검토를 거친다. 잔존 리스크(에이전트 Bash의
   상태 파일 접근)는 PC-7의 PreToolUse 훅이 커버하되, 보장 수준은 PC-8 결과로 확정된다.
2. **보존** — read-heavy 대량 요약은 종전대로 `gtask`(사슬 유지), 워크플로는 Claude 정밀도가 필요한
   판정·검증·교차검증 단계 전용. 비용 증가는 허용 커맨드 실행 시에만 **+0.4M 하한/실행** [실측
   콜드 스타트 기반 추정].
3. **유지** — 읽기 전용 워크플로는 diff를 생성하지 않으므로 압축 문제 자체가 없다. 커밋은 종전대로
   메인 세션의 단일 diff. (수정형은 이 선택지 범위 밖 — (B) 게이트로 미룸.)
4. **구조 계층 배치**: ① 워크플로 허용 커맨드에만 해당 도구를 여는 `allowed-tools`
   frontmatter(현재 19개 커맨드 전부 부재 — 신설), ② 전역 PreToolUse 훅으로 파괴적 Bash 패턴 차단
   (호출 주체 무관), ③ 커밋 게이트는 기존 CI(`.arachne/verify.sh`·Actions). **스크립트 내부 선언에는
   어떤 강제 역할도 부여하지 않는다.**
5. **충돌 없음** — 슬래시 커맨드는 `AGENTS.md:12-14`가 이미 "Claude 전용 보충 계층"으로 선언한
   위치다. `AGENTS.md` 본문 무변경, 최종 게이트는 CI로 전 CLI 동일(`AGENTS.md:173-179` 유지).
6. **허용** — 근거: 관측 불가 계층(컨텍스트) 위에 얹는 것은 병렬 *수정*이 아니라 병렬 *보고서
   생성*이며, 게이트는 관측 가능한 계층(산출물의 사람 검토 + 인용 스팟체크)에 있다. 이 근거는
   수정형에는 성립하지 않으므로, 수정형 확장은 (B) 체크리스트 충족 전 금지.

## 결정 (채택안)

**(D)를 채택한다** — 읽기 전용·검증 워크플로에 한정해 즉시 허용하고, **수정형 워크플로 확장은
(B)의 선결조건 체크리스트 PC-1~PC-10 전항 충족을 게이트**로 한다.

- (C) 기각: 강제 불가능한 프롬프트 의존 규칙을 추가하며(4번), 준수를 관측할 수 없다(6번).
- (A) 기각: "도입 안 함"은 무결함 현상 유지가 아니다 — 현행 수동 병렬(worktree 다중 세션)이 이미
  C-05 오탐·B-11 유실을 내고 있고, 대규모 검증 수요는 존재한다.
- 3-lane 배치: 워크플로는 `docs/MULTI-CLI.md` §5.0 계층의 **② 실행 메커니즘(Claude 전용)**에
  위치한다. 오프로드 사슬(Gemini→Codex)은 워크플로 밖에서 종전대로, 커밋·통합·최종 판정은 메인
  세션 Claude 단독 — 3-lane 계약 불변, `AGENTS.md` 불변.
- 단 PC-1~PC-6(현행 결함 계열)은 도입과 무관하게 수리한다 — 감사 "권장 처리 순서" 1~3과 동일.

## 반증 조건 (이 결정이 틀렸음을 알게 되는 관측)

1. 읽기 전용 워크플로 실행 전후 비교에서 repo `git status`·상태 파일(`.claude/last-*`,
   `arachne-quota-state`, `~/.claude/sessions/*`)에 **변경 1건이라도** 관측되면 — 읽기 전용 경계가
   프롬프트 계층에서 지켜지지 않는 것. → 즉시 (A)로 회귀, PC-7·PC-8 완료 전 재허용 금지.
2. 도입 후 4주간 `atask-quota-warn` 기준 Claude 쿨다운 진입 빈도가 도입 전 4주 대비 **2배 이상** —
   "비용 증가는 검증 단계 한정" 전제가 틀림. → 워크플로 허용 커맨드 목록 축소.
3. 워크플로 산출 보고서에서 사람 검증이 치명 오류(허위 인용·잘못된 판정)를 **연속 2회** 적발 —
   관측 불가 계층 위의 보고서 생성 허용 근거(6번)가 틀림. → PC-9·PC-10 완료 전 워크플로 중단.
4. PC-8 실험에서 PreToolUse·PostToolUse가 서브에이전트 도구 호출에 **발화하지 않음**으로 확정 —
   구조 계층 이관 계획의 훅 축이 공허해짐. → 강제 지점을 CI 단독으로 재설계하는 ADR 개정(supersede).

## 결과

- 후속 task: 워크플로 허용 커맨드 목록과 압축 계약(synthesis·verify 판정표)을 커맨드 문서에 명문화,
  PC-1~PC-10을 task로 분해(`docs/task/`).
- `AGENTS.md`·타 CLI 어댑터 무변경 — SSOT 유지.
- 근거 감사: [[2026-08-22-harness-runtime-audit]] (B-01~B-11, C-01~C-10, Q1 "미해결 — 관측 수단
  부재" 판정 포함).
