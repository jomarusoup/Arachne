# AGENTS.md — 공유 코어 규약

> **이 파일은 단일 진실 공급원(SSOT)이다.** Claude Code · Gemini CLI · Codex CLI가
> 모두 이 한 파일을 가리킨다 (Claude는 `CLAUDE.md`에서 `@AGENTS.md` import,
> Codex/Gemini는 `~/.codex/AGENTS.md`·`~/.gemini/GEMINI.md` 심볼릭 링크).
>
> 여기에는 **CLI 무관한 공통 규약만** 담는다. 특정 도구 전용 기능(Claude의 훅·서브에이전트·
> 슬래시 커맨드·모델 라우팅 등)은 각 도구의 보충 파일(`CLAUDE.md` 등)에 둔다 — 그래야
> 한 파일만 고치면 세 도구가 동시에 갱신되고 드리프트가 구조적으로 차단된다.

---

## 1. 작업 원칙

- **탐색 우선** — 파일 전체를 무작정 읽지 않는다. `sgrep <키워드>`(또는 동등 검색)로 위치를
  먼저 찾고 해당 범위만 읽는다. 토큰·컨텍스트를 아낀다.
- **재사용 우선** — 새로 구현하기 전에 기존 코드·라이브러리·표준(man/POSIX/패키지)을 먼저 찾는다.
  80% 이상 해결하는 검증된 구현이 있으면 채택한다.
- **수정 전 보고** — 기능 추가·삭제 등 비자명한 변경 전에는 `[PLAN]`으로 의도를 먼저 알린다.
- **수정 후 검증** — 변경 후 반드시 문법·동작을 검증한 뒤에 커밋한다 (아래 7장).
- **사실대로 보고** — 테스트가 실패하면 출력과 함께 실패라고 말한다. 건너뛴 단계는 건너뛰었다고
  말한다. 검증되지 않은 것을 "완료"라고 하지 않는다.

## 2. 코딩 스타일

모든 소스 파일·함수에 헤더 주석을 작성한다. **형태는 동일하게, 주석 문자는 언어에 맞게.**

| 단계 | 형태 (C계열 / `#`계열) | 용도 |
| --- | --- | --- |
| 파일 헤더 | `/*###…###*/` 또는 `####…####` | 파일 메타 정보 |
| 함수 헤더 | `/*===…===*/` 또는 `#===…===` | 함수 역할·인자·반환 |
| 로직 블록 | `/*---…---*/` 또는 `#---…---` | 의미 있는 블록 |

- 파일 헤더 필드: `FILE NAME` / `DESCRIPTION` / `DATA` / `Modification`
- 함수 헤더 필드: `FUNCTION` / `DESCRIPTION` / `PARAMETERS` / `RETURNED`(void·None이면 생략)

### 네이밍

| 형식 | 예시 | 대상 |
| --- | --- | --- |
| `snake_case` | `task_count` | 지역 변수·함수 인자 |
| `g_SnakeCase` | `g_UserList` | 전역 변수 |
| `PascalCase` | `GetTaskList` | 함수·메서드·타입·클래스 |
| `SCREAMING_SNAKE_CASE` | `MAX_RETRY` | 상수·매크로·환경변수 |
| `camelCase` | `taskCount` | JS 객체 속성·JSON 키 |

- 이름 길이 최대 30자, 이름만으로 역할 추측 가능해야 함
- 단일 문자 변수 금지 (`i`→`ii`, `j`→`jj`). 임시값은 `ii`·`jj`·`tmp`·`len`만 허용
- 한 줄에 변수 하나만 선언, 전역 변수는 초기값 필수

### 포매팅

- 들여쓰기 4 스페이스 (탭 금지 — 단, Go·언어 공식 규칙이 탭이면 그에 따름)
- 줄 끝 공백 제거, 파일 끝 빈 줄 1개
- 디버그 출력 prefix: 임시용 `[DEBUG]`(배포 전 제거), 운영 경고 `[프로젝트명]`

> 언어별 세부 규칙(중괄호 스타일·임포트 순서·포매터)은 `rules/<언어>/coding-style.md` 참고.

## 3. 설계 패턴

- **단일 책임(SRP)** — 함수·파일은 한 역할만. 함수 50줄·파일 800줄 초과는 분리 *점검 트리거*
  (역할이 하나면 줄 수 무관 통과). 중첩 4단계 초과 금지.
- **불변성** — 기존 객체를 변경하지 말고 새 객체를 만든다. 숨은 부작용을 막는다.
- **작은 파일 다수 > 큰 파일 소수** — 도메인·책임 단위로 구성(200~400줄 권장).
- **에러 처리** — 모든 단계에서 명시적으로. 사용자에겐 이해 가능한 메시지, 로그엔 상세 컨텍스트.
  실패를 조용히 삼키지 않는다.
- **입력 검증** — 시스템 경계에서 모든 외부 입력(API·사용자·파일)을 검증한다. 빠르게 실패한다.
- **하드코딩 금지** — 매직 값은 상수·설정으로.

## 4. 보안 (커밋 전 체크리스트)

- [ ] 하드코딩된 비밀값 없음 (API 키·비밀번호·토큰)
- [ ] 모든 사용자 입력 검증
- [ ] SQL 인젝션 방지 (파라미터화 쿼리)
- [ ] XSS 방지 (출력 이스케이프)
- [ ] 인증·인가 검증, 에러 메시지에 민감 정보 미포함
- [ ] 의존성 취약점 점검

비밀값은 환경변수·시크릿 매니저로만 다룬다. 노출 시 즉시 교체.
저수준 코드는 추가: 버퍼 오버플로(`strcpy`→`strncpy`), 포맷 스트링, 정수 오버플로, 권한 최소화.

## 5. 테스트

- **TDD** — RED(실패 테스트) → GREEN(최소 구현) → REFACTOR
- **AAA** — Arrange / Act / Assert 구조
- **커버리지 80%+** — 핵심 경로는 100%
- **네이밍** — 동작 설명형 (`연결_실패시_재시도_3회`, `NULL_입력시_에러_반환`)
- 단위·통합·E2E를 모두 둔다. 시스템 코드는 메모리 검사(valgrind/ASan/TSan) 추가.

## 6. Git 워크플로

커밋 메시지: `<type>: <설명>` + (선택) 본문.

| type | 의미 | | type | 의미 |
| --- | --- | --- | --- | --- |
| `feat` | 신규 기능 | | `test` | 테스트 |
| `fix` | 버그 수정 | | `chore` | 빌드·설정·의존성 |
| `refactor` | 동작 불변 정리 | | `perf` | 성능 개선 |
| `docs` | 문서 | | `style` | 포맷·스타일 |

- 브랜치: `main`(항상 통과 상태) / `feat/<기능>` / `fix/<버그>` / `refactor/<대상>`
- PR: `git diff [base]...HEAD` 분석 → 변경 요약 + 테스트 플랜 체크리스트

## 7. 검증 → 커밋 순서

수정 완료 시 아래 순서로 진행:

```
검증(문법·빌드·테스트) → git add → commit → (요청/관습에 따라) push
```

언어별 정적 검증 예: `gcc -fsyntax-only -Wall` · `go build && go vet` · `tsc --noEmit` ·
`ruff check` · `bash -n && shellcheck`. 공통으로 테스트 스위트 실행.

## 8. 이슈 처리 (타입별 원칙)

| 타입 | 핵심 |
| --- | --- |
| `[feat]` | 3파일+ / 신규 모듈 / 시스템 변경은 설계 선행. 단계적 구현(Phase 1→2→3) |
| `[bug]` | 재현 조건 먼저 → 근본 원인 분리 → 최소 수정 → 회귀 방지 |
| `[refactor]` | 동작 보존 전제. SRP 기준 분리. 점진적·검증 가능 단위 |
| `[remove]` | 참조 전수 조사 → 저장소 영향 확인 → 하위 호환성 처리 |

## 9. 언어별 상세 규칙

언어별 `coding-style · patterns · security · testing · hooks` 상세는 `rules/<언어>/`에 있다
(C · C++ · Go · Rust · Python · JS/TS · Bash). 해당 언어 작업 시 그 디렉토리를 참조한다.

도메인 워크플로 지식(저지연 시스템·트레이딩·테스팅·프론트엔드 등)은 `skills/`에 있다.

---

> **도구별 보충**: 이 공유 코어 외에, 각 도구는 자기만의 보충 설정을 가진다.
> 예) Claude Code는 `CLAUDE.md`에 서브에이전트·이벤트 훅·슬래시 커맨드·모델 라우팅·
> Gemini 위임(`gask`)을 추가로 정의한다. 그 내용은 다른 도구와 공유되지 않으므로
> 이 파일에 넣지 않는다.
