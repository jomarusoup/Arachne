# Python profile. The project owns pyproject.toml and uv.lock.
git diff --check
uv sync --frozen
uv run ruff format --check .
uv run ruff check .
uv run mypy .
uv run pytest
uv run pip-audit

# Optional DB gate for SQLAlchemy/Alembic projects (docs/DATA-HANDLING.md).
# uv run alembic upgrade head
# uv run pytest tests/data_contract -q
