# Web profile. The project owns package.json and pnpm-lock.yaml.
git diff --check
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm build
# CI runners start without browsers. Locally run `pnpm exec playwright install` once.
if [ -n "${CI:-}" ]; then pnpm exec playwright install --with-deps; fi
pnpm exec playwright test
